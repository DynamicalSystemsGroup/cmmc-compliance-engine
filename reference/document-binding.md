# Document ↔ engine binding (Rosetta)

How the two authored documents bind to the graph the engine builds. This is
the glue between what the compliance team writes and what the modules produce.

## Document 1 — Control Requirements Catalog → `<ce:ontology>`
[`control-catalog.md`](control-catalog.md) → [`../ontology/cmmc-edit.ttl`](../ontology/cmmc-edit.ttl)

| Catalog column | `cmmc:` property | Notes |
| --- | --- | --- |
| Control ID | `sysml:declaredName` / `cmmc:controlId` | e.g. `IA.L2-3.5.3` |
| NIST requirement text | `sysml:text` | verbatim official statement |
| CMMC weight | `cmmc:weight` | 1 / 3 / 5 (drives the SPRS oracle) |
| POA&M eligibility | `cmmc:poamEligible` | can it be deferred? |
| Non-deferrable flag | `cmmc:nonDeferrable` | must be MET before attestation |
| Assessment method | `cmmc:assessmentMethod` | Examine / Interview / Test |
| Evidence type | `cmmc:evidenceType` | kind of proof |
| Typical evidence artifacts | prose / `cmmc:evidenceHint` | exports, policies, logs |

## Document 2 — Traceability Matrix → `<ce:evidence>` + `<ce:attestations>` + `<ce:audit>`
[`traceability-matrix.md`](traceability-matrix.md) → generated by the SSP compiler in [`../documents/`](../documents/)

| Matrix column | Graph representation | Produced by |
| --- | --- | --- |
| Control ID | `rtm:addresses cmmc:<id>` | `evidence/binding.py` |
| Tier 1 implementation | `sysml:satisfyingElement` (control→resource) | `structural/` |
| Responsible party | `prov:qualifiedAssociation` → `prov:hadRole` | `traceability/attestation.py` |
| Evidence location | `rtm:documentRef` into the registry | `backends/` |
| Evidence hash / artifact ID | `rtm:contentHash "sha256:…"` | `evidence/hashing.py` |
| Status | `rtm:hasOutcome` → `earl:passed`(MET) / `failed`(NOT MET) / `inapplicable`(N/A) / `untested`(Planned) | attestation |
| Gap notes | `gsn:Justification` on a non-passed attestation | attestation |
| POA&M reference | `cmmc:poamItem` (legal only if `cmmc:poamEligible`) | `traceability/sprs.py` gate |

**The join.** Doc 1 (law) + Doc 2 (evidence) over one graph = the SSP + the
SPRS submission + the BOM + the C3PAO package. Today Doc 2 is authored by hand
with `Status: Planned`; the end state generates it from live evidence so it
cannot drift.
